# Start date of countdown (day 0 = 100); used when user has no start_date
start_date: "2025-03-05"

# Recipients (Telegram user IDs)
users:
  - id: 123456789 # user1
  - id: 987654321 # user2

# Timezone for "today" (default: Asia/Almaty = Kazakhstan)
timezone: "Asia/Almaty"
